cmake_minimum_required(VERSION 3.7)
project(StockViewer)

#find_package()
#include_directories(${CURSES_INCLUDE_DIRS})

include_directories(include)

set(CMAKE_CXX_STANDARD 17)

#add_definitions(-std=c++17)

set(CXX_FLAGS "-Wall")
set(CMAKE_CXX_FLAGS, "${CXX_FLAGS}")

file(GLOB SOURCES "src/stock.cpp")


add_library(stock ${SOURCES})
target_link_libraries(stock)

add_executable(StockViewer main.cpp)
target_link_libraries(StockViewer stock)
